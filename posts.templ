package main

import "fmt"
// import "time"

// templ bookmarksTemplate() {
// 	@layout() {
// 		<div>
// 			Lorem ipsum dolor sit amet, consectetur adipisicing elit. Unde impedit, laboriosam
// 			eum quisquam eveniet odit amet placeat iste ea perferendis sunt nostrum, voluptatum
// 			at quam excepturi quae rerum magni nobis?
// 		</div>
// 	}
// }
templ movieThumbnail(movie Movie) {
	<li id={ fmt.Sprintf("movieThumbnail-%d", movie.id) } class="thumbnail">
		<img class="thumbnail__img" src="/static/img/default-movie.jpg" alt="obrazek"/>
		<div class="thumbnail__content">
			<h2>{ movie.title }</h2>
			<p>{ movie.release_date }</p>
			<p>{ movie.vote_average }</p>
			if movie.bookmarked {
				<button
					hx-get={ string(templ.URL(fmt.Sprintf("/unsaved/%d", movie.id)))}
					hx-trigger="click consume" 
					hx-target={ fmt.Sprintf("#movieThumbnail-%d", movie.id) }
					>
					delete from bookmark
				</button>
			} else {
				<button hx-get={ string(templ.URL(fmt.Sprintf("/saved/%d", movie.id)))} hx-trigger="click consume" hx-target={ fmt.Sprintf("#movieThumbnail-%d", movie.id) } hx-swap="outerHTML">add to bookmark</button>			
			}
		</div>
	</li>
}

func sendID(id int) string {
    return fmt.Sprintf(`{'thumbnailId': '%s'}`, id) 
}
func sendIDGET(id int) string {
    return fmt.Sprintf(`/saved?id='%s'}`, id) 
}

templ searchTabTemplate(movies []Movie) {
	<div>
		<ul class="movies-thumbnails" id="searchResultTemplate">
			for _, movie := range movies {
				@movieThumbnail(movie)
			}
		</ul>
	</div>
}

templ Home() {
	<html>
		<head>
			<title>test</title>
			<script src="https://unpkg.com/htmx.org@1.9.12"></script>
			<link href="/static/css/output.css" rel="stylesheet"/>
		</head>
		<body>
			<header class="header" data-testid="headerTemplate">
				<h1 class="header__title">Name of the Page</h1>
				<form class="search" hx-post="/search" hx-target="#main" hx-swap="innerHTML">
					<input class="search__input" type="text" name="search" placeholder="Search"/>
				</form>
				<nav class="nav" data-testid="navTemplate">
					<ul class="nav__list">
						<li><a href="/">Home</a></li>
						<li><a href="/searchTab">Search</a></li>
						<li><a href="/bookmarksTab">Watchlist</a></li>
					</ul>
				</nav>
			</header>
			<main id="main">
				{ children... }
			</main>
		</body>
		<footer data-testid="footerTemplate">
			<p>
				Lorem ipsum dolor sit amet consectetur adipisicing elit. Libero soluta labore quas quaerat ipsa adipisci ducimus dolores architecto? Omnis, consectetur ea vitae blanditiis voluptatum repudiandae libero optio iste delectus dolores!
			</p>
		</footer>
	</html>
}
